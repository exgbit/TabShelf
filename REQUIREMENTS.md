# Toby 替代版 —— 需求文档

## 背景

Toby 转为付费后，自研一个免费、开源、无后端的浏览器标签管理扩展，覆盖其最核心的功能。

## 目标

- 免费开源，数据不出浏览器（无自建后端）
- 覆盖 Toby 的三大核心能力：保存/恢复标签组、新标签页工作台、全局搜索
- 代码尽量简单，无构建工具，可直接加载调试

## 平台与技术栈

- **平台**：Chrome（Manifest V3），天然兼容 Edge；不兼容 Firefox/Safari
- **技术栈**：原生 JavaScript + HTML + CSS，无框架、无打包器
- **存储**：`chrome.storage.local` 纯本地，不经过任何云端
  - 跨设备迁移通过导出/导入 JSON 文件完成（用户核心诉求，见 F4）

## 功能需求

### F1 保存 / 恢复标签组

- F1.1 点击扩展图标弹出保存面板（Toby 弹窗风格）：「保存标签」把当前标签快捷存入最近使用的收藏集（无则自动建「收集箱」）；「保存窗口」把当前窗口全部标签存为新收藏集；均可选保存后关闭标签
- F1.2 弹窗头部可切换组织（记住上次选择）；SPACES 列表仅显示当前组织的空间，采用逐级钻取：空间行悬停显示快捷保存图标，点击进入空间后列出收藏集（头部有返回、快捷保存、新建收藏集），点击收藏集即把当前标签存入；搜索时平铺显示匹配的空间和收藏集
- F1.3 工作台收藏集头部「打开全部」在新窗口恢复整组标签；工作台右栏 OPEN TABS 每个窗口也可一键保存为收藏集
- F1.4 收藏集支持重命名、删除
- F1.5 收藏集内单个标签支持单独打开、删除

### F2 新标签页工作台（Toby 风格四栏布局）

- F2.1 接管浏览器新标签页，四栏布局：组织图标栏（左一）、空间面板（左二）、收藏集卡片区（中间）、OPEN TABS（右栏）
- F2.2 收藏集带「组织 / 空间」两级归属，左栏可按组织、空间筛选；空间自动从收藏集归属中派生
- F2.3 收藏集为整宽分区 + 书签卡片网格，支持单个折叠、一键 EXPAND/COLLAPSE 全部
- F2.4 书签卡片显示 favicon、标题、域名；支持单个删除
- F2.5 右栏实时列出各窗口打开的标签（点击激活跳转），每个窗口可一键保存为收藏集
- F2.6 收藏集头部图标操作（悬停显示）：↗ 打开全部、⬇ 把当前窗口标签存入该收藏集、★ 星标切换（空间面板有「星标收藏集」入口）、⧉ 复制收藏集、🔗 复制全部链接到剪贴板、⋮ 菜单（重命名 / 移动到空间 / 删除）
- F2.7 拖拽排序：收藏集之间排序、卡片内标签排序、标签跨收藏集移动（待做）
- F2.8 手动添加链接（标题 + URL）到指定收藏集（待做）

### F3 全局搜索

- F3.1 工作台顶部搜索框，输入关键词实时过滤
- F3.2 搜索范围：已保存标签 + 当前浏览器已打开的标签（分开展示）
- F3.3 匹配字段：标题、URL
- F3.4 搜索结果点击后直接跳转（已打开标签则激活对应标签页）

### F4 数据管理

- F4.1 导出全部数据为 JSON 文件
- F4.2 从 JSON 文件导入（合并或覆盖可选）
- F4.3 兼容 Toby 官方导出 JSON：拍平「组织 / 空间 / 集合」层级为收藏集，名称保留路径，顺序按 position

## 非功能需求

- 隐私：不联网、不收集任何数据，所有数据仅存于浏览器本地存储
- 性能：新标签页打开即渲染，收藏集 500+ 时不卡顿
- 权限最小化：仅需 `tabs`、`storage`、`favicon`

## 明确不做（v1 范围外）

- 多窗口/工作区切换（Workona 式的 workspace 概念）
- 标签休眠（tab suspender）
- 团队协作、分享链接
- 笔记、待办、提醒
- AI 自动归类
- 云同步服务器

## 里程碑

- **M1 MVP**：F1 全部 + F2.1（只读展示）+ F3 + F4 导入导出，可日常替代 Toby 使用
- **M2**：F2 拖拽交互与手动添加链接
- **M3**：F4 导入导出、体验打磨（空状态、快捷键、深色模式）
